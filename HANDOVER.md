# THEMADLIONS Projects · handover

Read this first when continuing the work in a new conversation.

## What it is
Web-based film production app for The Mad Lions (Alex Konstantinidis, Athens). React + Vite, plain CSS (src/styles.css, theme variables), no UI framework. Frontend on GitHub Pages, backend on Supabase (auth, Postgres, Storage, Realtime).

- Repo: https://github.com/alexisdigipro-hub/themadlions-projects (owner: alexisdigipro-hub)
- Live: https://alexisdigipro-hub.github.io/themadlions-projects/ (auto-deploys on push to main via .github/workflows/deploy.yml, ~40 s)
- Supabase project: https://naibamqexcqnqhbbqafa.supabase.co (publishable key in src/lib/supabaseConfig.js; build with VITE_LOCAL_MODE=1 for a local-storage build used in tests)
- Work continues in Claude Code (since 18 Sep 2026), see CLAUDE.md. Local sessions push to main with Alex's own git login; cloud sessions push a branch and Alex merges the PR. No GitHub tokens in chats, ever.

## Architecture in one paragraph
Every project is one JSON document (projects.data). Events, library (people, locations, general tasks), finance (admin-only) and members are their own tables. src/lib/store.jsx holds the whole state, persists to localStorage in local mode or syncs diffs to Supabase (debounced upserts, realtime merges). Pages call `update(fn)` / `updateProject(id, fn)` and mutate a draft. Permissions: per user, per module, view/edit, plus project access; RLS enforces membership, project access and edit rights.

## SQL files Alex must have run in Supabase (SQL Editor), in order
schema.sql (core), storage.sql (photos), library.sql (company library + storage policy update), todos.sql (general tasks), finance.sql (admin-only finance, recurring), audio.sql (song files), files.sql (project files), chat.sql (team chat); telegram-remove.sql drops the old Telegram mirror if it was ever created, notices.sql (targeted pop-up notices with confirmations), worklog.sql (personal work log), drives.sql (adds kind 'drive' to the library table for the drives archive), shares.sql (public share links + share_get RPC for anonymous readers), activity.sql (activity log, admins read, 180-day prune). All are safe to re-run.

## Modules done
Projects (6 categories in this order: Music Video, Event, Editing, Ad, Visuals, IV; old names Events/Feature Film/Advertise are migrated on load; tall cards with square covers), script import + revisions (colours, compare), rule-based Greek/English scene detection, AI breakdown (Claude, BYO key in Settings), treatment/moodboard breakdown (PDF, images), keyword element hints, shot list + storyboard, stripboard + Day Out of Days, call sheets (StudioBinder-style layout, sun/weather, sides, Send message via WhatsApp/mail/copy), tasks (project + general, department chips), production reports, budget (cap bar, commitments, payments), equipment & vendors, post & deliverables, company Database page in the sidebar with tabs Locations / Crew / Cast (old /people and /locations routes redirect there), notices (admins send a pop-up to the whole team or chosen members from Chat > Send notice; it shows as a modal until the person taps Got it; senders see who confirmed under Sent notices; notices table), team Chat in the sidebar (messages table, realtime, unread badge), photo galleries (browser compression), Music tab (waveform, song map, Whisper lyrics/timing with BYO OpenAI key), Finance (admin-only: transactions, VAT, tax estimate, recurring, budget commitments), Home overview (mini calendar with day picker instead of a This week list), Events run of show, project covers, progress bars, light/dark themes with accents, text size.

- My work (sidebar, every non-admin member; Alex asked that administrators do not get it, they only see Team work in Finance): personal job log modelled on the crew's Google Sheet (status Paid/Pending, client, description, amount, shooting date, paid date and method, notes, optional project link), grouped by month with year tabs, totals, CSV. Admins see everyone under Finance > Team work (per-member cards, totals owed/paid, open a member to edit or mark paid). Table worklog, RLS own rows or admin.

- Drives archive (sidebar, permission module 'drives' in Team so Alex picks who sees it; existing members default to none): disks grouped by series (LION, SIMBA, TML…), capacity, free space, status (empty / in use / full / not around), where it is, and the list of projects on each disk (free title or a link to an app project); search answers "which disk has X". Stored as library rows kind 'drive'.
- Team: deactivated members can now be removed for good (members row deleted; their auth account stays but has no workspace).

- Call sheet Share link: Call sheets > Share link publishes a snapshot (no department requirements, no budget) to the shares table and gives a public URL /#/s/<token> that opens without login on a mobile-first page (PublicCallSheet.jsx: big call, times, weather, pinned note, location with Directions, cast+crew calls with name search, scenes, production contacts). Same link per day, re-sharing refreshes it. Anonymous read goes through the share_get() function only.

- Settings (admin): company logo (data URL in settings.logo, resized to 320px; shown in sidebar, login, call sheet header, shared link), call sheet defaults (settings.callsheet: crew call, wrap, lunch N hours after call, tagline, parking, hospital, footer; new shooting days and empty call sheet fields fall back to them), editable department list (settings.departments, used by crew, database, tasks via departmentsOf()), team rules (notices are admin-only, share links open to everyone; settings.newMemberLevel default permission for new teammates; settings.phoneVisibility everyone|admins hides cast/crew phones and emails inside the app via canSeeContacts(); settings.noticeVibrate), share link expiry (settings.shareExpiryDays, stamped into the snapshot as expiresAt), calendar week start (settings.weekStart, monthGrid/weekdayShort take it), default project category. Also: progress stages per category (settings.progress[cat] = {off, weights, custom}; projectProgress(p, settings); manual custom stages are ticked on the Overview into project.customStages), project lock (project.frozen; canEdit false for non-admins; Lock/Unlock link on Overview), budget currency/contingency defaults, default cast/crew call offsets, weather/sun toggles on call sheets, test notice button, drives archive 6 per row with series order in settings.driveSeriesOrder (arrows next to each series title). Settings page is tabbed: Company, Call sheets, Team, Calendar & projects, Display, Integrations, Data (sections carry data-tab, CSS shows the active one). Still planned: admin password reset (needs SMTP or an Edge Function), automatic notices (new chat message, new My work job, overdue task).

- Activity log: store.jsx logActivity() writes to the activity table from syncDiff (project created/updated with the changed sections merged per 4s, deleted, locked/unlocked; events; member removal). Settings > Data > Activity lists the latest 200 with a filter.

## Where we stopped (18 Sep 2026)
- **Home blocks can be reordered.** Home.jsx was restructured from a fixed sequence of sections into a `blocks` map (team, calendar, attention, projects, status, types, finance) rendered in whatever order the person chose. An Arrange button in the page head turns on dragging plus up/down arrows per block, with Reset order; Done turns it off again. Order is kept in localStorage under `tml_home_order`, matching how theme and text size are already per-device, so a phone and a desktop can differ. Two pure helpers are exported and were tested: readHomeOrder() (survives missing, corrupt or wrong-shaped storage, drops blocks that no longer exist, appends blocks added later so a new one never vanishes, collapses duplicates) and reorder(). Arrows are there because HTML5 drag and drop does not work on touch.
- Layout note: .home-blocks uses explicit grid tracks (3 / 2 / 1 by width) rather than auto-fit, because a full-width block spans `1 / -1` and auto-fit collapses the empty tracks that span depends on. The old .home-grid rule is gone, nothing used it any more.
- Not built and not asked for: hiding a block rather than only moving it. Easy now that the blocks map exists, say the word.
- Team strip on Home tuned to Alex's note: faces doubled (92px desktop, 80px mobile, chips and the ✕ scaled with them) and the "The team" heading removed, leaving only the muted line that says which day is shown and how many are away. The strip now renders profile.photo (320px) rather than profile.thumb (96px), which would have looked soft at that size on a retina screen; the thumb is still what the Team rows and the chat avatars use. No re-upload needed, both sizes were already stored.
- **Team strip on Home.** A row of everyone at the top of Home: profile photo (initials as a fallback), first name, click to open that person's profile (/me for yourself). Anyone not available on the day being viewed is greyed out, crossed through and struck out. It uses what already existed: days off are calendar events of type 'unavailable' carrying createdBy, so nothing new had to be stored. unavailableOn(events, iso) in store.jsx is the lookup and handles ranges inclusively at both ends; verified against single days, mid-range, both edges, the day after, and events of other types or with no author.
- MiniCalendar gained an optional onSelect prop, so Home follows the day you pick there instead of being stuck on today. Every other MiniCalendar stays exactly as it was, since the prop is optional.
- Nothing to run in Supabase for this one.
- **Member profiles.** New page src/pages/Profile.jsx serving two routes: /me (My profile, in the sidebar for everyone) and /u/:id (someone else's). Fields Alex picked: photo, position, department (from settings.departments), phone, a few words, birthday. Editable by the person themselves and by administrators. Name and email stay administrator-managed in Team, because the write path for a member only covers the profile column.
- Privacy, as Alex chose: each member decides whether the whole team or only administrators see their **phone and birthday** (profile.showPhone). Name, photo, position and bio are always visible to the team. canSeeProfileDetails() in Profile.jsx is the single rule, verified against 7 cases.
- **NEW SQL FOR ALEX TO RUN: supabase/profiles.sql.** It adds members.profile jsonb and a set_my_profile(jsonb) SECURITY DEFINER function. This matters: members_admin is the only write policy on members, and that row also holds role, permissions and project_access, so a plain "update your own row" policy would let anyone make themselves an administrator. The function writes only the profile column of the caller's own row. store.jsx routes a non-admin editing themselves through the RPC and everyone else through the normal members update, and both paths report "Run supabase/profiles.sql" if the column or function is missing.
- The photo is a data URL inside the member row (320px, plus a 96px thumb), same approach as the company logo, so profiles need no storage bucket.
- Entry points: My profile in the sidebar; Team rows show the photo and link to the profile (administrators only, since Team is admin-only); Chat shows the photo beside each message and the name links to the profile, which is how a non-administrator reaches anyone else's. Alex picked only the sidebar item, so the Chat link and the /u/:id page were added to make his own privacy choice meaningful. Say if they should go.
- Not built: a Team directory page with everyone in cards, and clicking names in tasks or Team work. Both were offered and not picked.
- Seven settings Alex picked, all built and all off the "still planned" list:
  1. **Automatic notices** (settings.autoNotice): a notice when a task is assigned to you, and one for a new chat message. sendAutoNotice() in Notices.jsx keeps the sender as the person who caused it, which is what the notices RLS policy requires, and collapses repeats by key into the notice still unread, so twenty chat messages are one pop-up that says twenty. A notice already confirmed is never reused. An absent switch counts as on, matching what Settings shows.
  2. **Emergency numbers** (settings.emergency, default Ambulance 166 and Fire brigade 199, editable in Settings > Call sheets): printed on every call sheet and carried into the public share link, tap to dial.
  3. **Production contacts** (settings.productionContacts): role, name, phone, shown under Production on every call sheet above the key crew of that project.
  4. **Project code** (settings.projectCodePrefix, default TML): new projects get TML-2026-001, counted per year. nextProjectCode() in store.jsx derives the number from the codes already in use rather than a stored counter, so two people creating projects at once cannot collide. Shown on the project card and the project header, and searchable. Empty prefix turns codes off.
  5. **Greek public holidays** (settings.greekHolidays): all 13, with the movable ones off Orthodox Easter. greekHolidays()/holidayName() in dates.js; orthodoxEaster() verified against 2023-2030. Shown in the month calendar and the mini calendar.
  6. **AI language** (settings.aiLanguage, greek or english): the breakdown prompts in ai.js were hard-wired to Greek; both system prompts and both user prompts now take the language, including the flag examples.
  7. **Financial year start** (finance.settings.fiscalYearStart) is no longer dead. fiscalYearOf()/fiscalYearLabel() in finance.js, exposed in Finance settings, and every year grouping on the Finance page goes through it. January keeps behaving exactly as before, so nothing changes unless Alex changes it.
- Nothing to run in Supabase for any of this. Settings live in the workspace row and both loaders merge new keys over the defaults, so existing workspaces pick them up on next load.
- Still open, discussed but not decided: crew cost per project from the My work project link (Team work or inside each project's Budget), the iOS theme, the presentation tab, the production sheet view in Breakdown, admin password reset, PWA install and push.
- Finance > Team work rebuilt around what Alex actually does there. New **Owed right now** panel at the top of the tab: every unpaid job from every year in one list, longest waiting first, each row showing who, what, the date, how many days it has waited and a Mark paid button, over four totals (up to 30 days / 31 to 60 / over 60 / all unpaid). The list is capped at 8 rows with a Show all link. Aging thresholds and the shared paid toggle live in src/components/WorkLog.jsx (daysWaiting, AGE_BUCKETS, ageBucket, togglePaidEntry), so My work and Team work mark a job paid the same way.
- FIXED in the same pass: the Team work figures did not add up. The hero cards summed every worklog row while the person cards below listed only active non-administrators, so money belonging to anyone deactivated, promoted to admin or removed from Team counted in the big numbers with no card to open. In a test case the hero read 2490 and the cards added to 950. The page now builds its people list from the active team plus anyone who still has jobs logged (labelled "not in the active team any more", or "Not assigned" for rows with no user), and the hero counts exactly those people. Verified with a script covering an admin, a deactivated member, a removed member and an entry with no user.
- Also in Team work: the bar inside each person card used to take its width from the biggest earner while looking like a progress bar. It is now a fill whose length compares the person with the year's biggest and whose internal split is that person's own paid vs pending, with a tooltip saying so.
- FIXED, was live and broken: the Call sheets page of every project crashed the moment it opened. In src/pages/project/CallSheets.jsx the `lunchDefault` value was computed on line 34, above `const day` on line 37, and it reads `day.callTime`. A `const` cannot be read before its declaration, so React threw `ReferenceError: Cannot access 'day' before initialization` on every render. Introduced by commit 50f90a8 (the call sheet defaults work). `npm run build` did not catch it, because it is a runtime error and not a syntax error, which is why the deploy stayed green while the page was dead. `lunchDefault` now sits after `day` and reads `day?.callTime`, so the no-shoot-days case is safe too. There is exactly one immediately invoked function in the codebase and this was it.
- .github/workflows/deploy.yml now also builds every pull request into main (`npm ci` + `npm run build`, no deploy), so a broken commit shows red on the PR instead of after the merge. Before this the workflow ran only on push to main, so pull requests had no checks at all. The deploy job is guarded with `if: github.event_name != 'pull_request'` and holds the pages/id-token permissions on its own; the top level is now `contents: read`. The concurrency group is per ref, so a PR build can never cancel a live deploy.
- Audit of the repo in a cloud session: syntax clean across all of src, every relative import resolves, the CSS for the new Breakdown paste box and Shots manual setups is in place, no leftover console.log, no credentials in the repo. One real bug found and fixed: library.sql and todos.sql still wrote the library kind list without 'drive', so re-running either of them after drives.sql would fail once a drive existed. All three files now carry the same list ('contact', 'location', 'task', 'drive'), so any of them can be re-run in any order. Alex does not have to re-run anything for this unless a drive insert complains, in which case running supabase/drives.sql once fixes it.
- Note for cloud sessions: registry.npmjs.org is blocked by the environment network settings, so `npm ci` and `npm run build` cannot run here. Until it is allowed, the GitHub Actions build on push to main is the only real build check.
- Commits pushed from outside this chat on 18 Sep ("Add files via upload"): Breakdown gained a paste-a-treatment panel (paste text straight into the page and run the AI breakdown), Shots gained manual setups from the rail and quick-add shots under a scene, small TasksAll tweak. Merged, builds clean.
- SQL files Alex still has to confirm he ran: notices.sql, worklog.sql, drives.sql, shares.sql, activity.sql (chat.sql is confirmed). If a feature complains, that is why.
- Team signups: Confirm email is now OFF in Supabase Auth. Members must be added in Team with the exact email they registered with, then they reopen the app.
- Elias Karatzogiannis: elias-2026.csv was prepared for import into his My work (Finance > Team work > his card > Import CSV); not confirmed imported.
- Categories: "Ad" was Alex's "Add" (confirmed as Ad in practice); "IV" still not explained by Alex, currently behaves like Ad and sits last.
- Next thing Alex asked about: an iOS-style UI theme (third theme next to Light/Dark: SF-like font, grouped lists, borderless buttons, toggles, segmented controls, large titles, Back button; call sheet print view stays as is). Not started. Open question: keep amber accent or go iOS blue.
- Also discussed, not built: Presentation tab inside projects from Alex's template (he has not sent it), production sheet view in Breakdown (Google Sheet-style scene x character grid; agreed useful, not started), admin password reset, automatic notices, PWA install + push.

## Not done / ideas discussed
- Call sheet delivery with confirmation (email via Resend Edge Function, WhatsApp Business API; Alex dropped Telegram). Currently Send message opens WhatsApp/mail with prefilled text, and Share link gives a public page.
- Chat: no push notifications yet (only the sidebar badge while the app is open).
- pCloud/Drive deep integration (browse folders in app). Currently: file uploads to Supabase bucket "files" (50 MB/file free plan) + per-project cloud folder link.
- Import of the old finance Google Sheet as history (needs a CSV from Alex).
- Move AI keys server-side (Edge Function) so teammates share them.

## Mobile layer
Everything under 820px lives in the last block of src/styles.css ("mobile app layer") plus useIsMobile() in ui.jsx: bottom tab bar (Home, Projects, Calendar, Chat, More opens the sidebar), 3 project cards per row with chips in one scrolling row, the Calendar page swaps the big grid for the MiniCalendar (dots + day list, tap a day to add). Alex wants desktop untouched when changing mobile.

## Conventions
UI text in English; Alex talks in Greek; no em dashes in replies to him. Test with Playwright against `npx vite preview --outDir dist-local` (see /tmp flow scripts pattern in past work: create workspace, add sample project, drive the UI). Keep everything in src/styles.css; theme via html[data-theme] and html[data-accent].
